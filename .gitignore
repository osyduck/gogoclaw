# Captured traffic — contains real access/refresh tokens + PII. Never commit.
*.har

# Superpowers scratch (briefs, reports, review diffs, progress ledger)
.superpowers/

# Go build artifacts
/gogoclaw
/gogoclaw.exe
*.exe
*.test
*.out

# SQLite runtime database (holds live bearer tokens)
accounts.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
Thumbs.db

# Frontend deps
web/node_modules/

# tsc -b project-reference build artifacts (tsconfig.node.json has no
# emit target for vite.config.ts; TS emits alongside the source instead)
web/*.tsbuildinfo
web/vite.config.js
web/vite.config.d.ts
