# Captured traffic — contains real access/refresh tokens + PII. Never commit.
*.har

# Superpowers scratch (briefs, reports, review diffs, progress ledger)
.superpowers/

# Playwright MCP capture artifacts (page snapshots, console logs)
.playwright-mcp/

# Go build artifacts
/gogoclaw
/gogoclaw.exe
*.exe
*.exe~
*.test
*.out

# SQLite runtime database (holds live bearer tokens)
accounts.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
Thumbs.db

# Frontend deps
web/node_modules/

# TypeScript incremental build cache (tsc -b emits these even with noEmit)
*.tsbuildinfo

# Python
__pycache__/
.pytest_cache/
sidecar/.venv/

